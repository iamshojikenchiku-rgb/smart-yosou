# Smart予想ツール 使い方マニュアル v1.0

対象: `index.html`（A〜F層＋G層π参考値・結果入力・バックテスト用エクスポート搭載版、main 1ca9f5a 以降）
公開URL: https://iamshojikenchiku-rgb.github.io/smart-yosou/
主端末: Android タブレット（ホーム画面に追加した PWA）。PC のブラウザでも同じ。
最終更新: 2026-09-13

---

## 0. 先に守ること（鉄則）

1. **ツール判定は絶対。** 🛑 / 🚫 / ⚠️ が出たら理由を問わず見送り。
2. **全頭入力。** 下位人気も省略しない（穴馬の取りこぼしが過去の失敗パターン）。
3. **推定人気（推:）は SmartRc の値。** ツールが計算するものではない。
4. **予算**: 月 10,000 円／週 2,500 円（勝負所のみ 3,000 円）。1票 100 円。
5. **確定着順・確定馬場・払戻は結果画面のスクショから入力。** 記憶や検索で埋めない。
6. G層「π参考値」は**表示のみ**。買い目には使わない（バックテストで有効性が確認されるまで）。

---

## 1. 画面の全体像

```
┌ ヘッダー ─────────────────────────────────────────┐
│ Smart予想  スクショから3分で買い目   [取込][統計][保存][リセット] │
└────────────────────────────────────────────────┘
 Step1 レース情報  →  Step2 出走馬入力  →  Step3 判定結果
```

| ボタン | 役割 |
|:--|:--|
| 取込 | Claude が作った JSON を貼り付けて一括入力（**通常はこれだけ使う**） |
| 統計 | 結果入力済みレースの回収率・的中率・荒れスコア帯別・月別推移 |
| 保存（一覧） | 保存済みレース一覧。読込／結果入力／削除／**バックテスト用 JSON コピー** |
| リセット | 入力を全消去（保存済みレースは消えない） |

データはすべて端末のブラウザ内（localStorage）に保存されます。GitHub には送られません。
**ブラウザのサイトデータを消すと保存済みレースも消えます**（§7）。

---

## 2. 週の流れ（どこでツールを使うか）

| いつ | 誰 | 何を | ツールの操作 |
|:--|:--|:--|:--|
| 木曜夜 | Claude（J1 自動） | 候補レースを出典付きで提示 | — |
| 金曜〜土曜朝 | 人 | 候補を承認し、SmartRc の出馬表・血統・推定人気・複勝オッズのスクショをプロジェクトに投入（`YYYYMMDD_レース名_種別_連番.png`） | — |
| 土日 07:30 | Claude（J2 自動） | 全頭 JSON を作り judge.js で判定、買い目メモを通知 | — |
| 土日 午前 | 人 | メモを確認。迷ったらメモ末尾の JSON を **取込** に貼って同じ結果を再現 | 取込 → 分析 → 保存 |
| 土日 | 人 | 判定どおり購入（🛑🚫⚠️ は見送り） | — |
| レース後 | 人 | 結果スクショを投入し「結果記録」と依頼、または自分で **結果入力** | 保存一覧 → 結果 |
| 随時 | Claude（J3） | サンプル・台帳・バックテストを更新 | 保存一覧 → JSONをコピー |

J1/J2 が止まっている週や、自分で試したいときは §3〜§4 の手順で手入力できます。

---

## 3. JSON 取込（通常の入力方法）

1. ヘッダーの **取込** を押す。
2. Claude が返した JSON（`{ "race": {...}, "horses": [...] }`）をテキスト欄に貼る。
3. **読み込む →** を押すと Step3（判定結果）が直接開く。

### JSON の形（17キー・`odds_fuku` と `chichi_mei` は任意）

```json
{
  "race": { "name": "京成杯AH", "course": "中山", "distance": "1600", "track": "芝",
            "babaCondition": "良", "isAreru": false, "bias": "", "isHandicap": true,
            "raceClass": "G3", "courseType": "small_turf" },
  "horses": [
    { "uno": 1, "name": "○○", "est_pop": 3, "rank": "B", "odds": 6.5, "pop_tan": 2,
      "rider_change": false, "cond_change": false, "bonpaso": "",
      "chichi_kei": "ロベルト系", "chichi_mei": "モーリス",
      "haha_chichi_kei": "サンデー系", "hahahaa_kei": "未入力",
      "kishu_mei": "", "haha_seiseki": "不明", "sanka_sedai": "不明",
      "odds_fuku": 2.1 }
  ]
}
```

| キー | 意味 | 入力元 |
|:--|:--|:--|
| est_pop | 推定人気 | SmartRc |
| rank | 評価 A〜E | SmartRc |
| odds / pop_tan | 単勝オッズ／単勝人気 | 出馬表 |
| odds_fuku | 複勝オッズ**下限**（任意・G層用） | オッズ画面 |
| chichi_kei | 父小系統（SmartRc 表示名そのまま） | 血統画面 |
| chichi_mei | 種牡馬名（任意・F層が効く） | 血統画面 |
| haha_chichi_kei / hahahaa_kei | 母父・母母父の小系統 | 血統画面 |
| kishu_mei / haha_seiseki / sanka_sedai | 騎手名／母の実績／産駒世代 | 分かる範囲 |
| courseType | big_turf / small_turf / local_turf / yosiha / big_dirt / small_dirt / local_dirt | 会場から判断 |
| isAreru | 「荒れるレース判定」（荒れスコア +8） | ARERU_RACES 該当時のみ true |

`isAreru` を「判定を甘くする目的」で ON にしないこと。

---

## 4. 手入力する場合（Step1 → Step2 → Step3）

**Step1 レース情報**: レース名・コース・距離・芝/ダート・馬場（予想）・クラス・ハンデ・バイアス・コースタイプ・「荒れるレース判定」チェック。
画面下の「参考：荒れやすい主要レース」に月別一覧（備考付き、△はクラシック路線＝大穴控えめ）が出ます。
→ **次へ：出走馬入力 →**

**Step2 出走馬入力**: 1頭ずつ 馬番・馬名・推定人気・評価・単勝人気・単勝オッズ・**複勝オッズ下限（任意）**・乗り替わり・条件変化・前走凡走理由・血統（A層 父小系統／B層 母父・母母父／D層 騎手／E層 母実績／C層 産駒世代／種牡馬名）。
血統を入れると「🧬 血統スコア」のプレビューが出ます。最低 3 頭（買い目が出るのは 4 頭以上）。
→ **分析 →**

**Step3 判定結果**: §5 のとおり読む。**保存** で保存済みレースに入ります（結果入力に必要）。

---

## 5. 判定結果の読み方

### 5.1 荒れスコア（0〜100）
1番人気のオッズ、推定1位と実人気の一致、穴候補の数、過剰人気の数、頭数、ハンデ、クラス、馬場、荒れ判定、ダートなどの加減点。「スコア内訳を見る」で確認。
70以上 🔥大荒れ予兆／50〜 ⚡荒れ気味／30〜 📊標準／30未満 💎堅め。

### 5.2 軸信頼度
| 表示 | 条件（要旨） |
|:--|:--|
| 高（1頭軸推奨） | 1位が A/B 評価・推定3位以内・実人気5位以内・2位との差15点以上 |
| 中（2頭軸マルチ推奨） | 1位が A〜C・推定5位以内・差8点以上 |
| 低（フォーメーション推奨） | 上記以外 |

### 5.3 総合判定（荒れ × 軸信頼度）と予算ティア
| 判定 | 意味 | 目安 |
|:--|:--|:--|
| ⚡ 勝負所 | ①1位が2位に20点差 ②荒れスコア65以上 ③過剰人気警告 の3条件同時成立 | 3,000円 |
| 🎯 買い場（本命厚め） | 軸が固い | 1,500円 |
| 💎 お試し（少額穴狙い）／📊 通常運用／📌 オッズ見極め／💰 堅め単発 | 通常〜小額 | 500〜1,000円 |
| 🛑 見送り推奨／🚫 見送り推奨／⚠️ 慎重判断 | **買わない** | 0円 |

※ 助言文の金額と「推奨買い目」の合計が一致しない組み合わせがあります（既知・設計メモ §6）。**購入判断は判定の絵文字、金額は「推奨買い目」欄の合計**を基準にしてください。

### 5.4 過剰人気の警告
実人気 3 位以内なのに推定 7 位以下の馬。買い目から外す検討対象。

### 5.5 スコアランキング（1頭ずつのカード）
総合スコア／評価／推:（推定人気）人:（実人気）オッズ／根拠／妙味（乖離）／🧬 血統スコア（A〜F 層の内訳を展開可）。
カード内の **「π 3着内: xx% 公平複勝: x.xx倍 複勝: x.x倍 π候補／率のみ」** が G層π参考値（暫定・較正前）。
見送り判定の日は取り消し線でグレー表示＝参考外。**買い目には使わない。**

### 5.6 推奨買い目
券種・馬番・点数・金額・合計投資額。メイン（ワイド系）とギャンブル枠（3連単少額）。
当日の最終オッズで妙味が消えていれば減額・見送り可。増やす方向の変更はしない。

---

## 6. 保存・結果入力・統計・エクスポート

### 6.1 保存
Step3 の **保存** で保存済みレースに登録（荒れスコア・判定・買い目も一緒に保存）。

### 6.2 結果入力（レース後）
ヘッダー **保存** → 対象レースの **結果** → 1〜3着の馬番、**開催日**、**確定馬場**、単勝配当・主要配当（参考）、**購入額・払戻額**、メモ → **記録する**。
購入しなかった（見送り）レースも購入額 0 で記録すると、見送り判定の答え合わせになります。

### 6.3 統計
ヘッダー **統計** → 累計購入・払戻・回収率・的中率、荒れスコア帯別の成績、TOP配当、月別推移。

### 6.4 バックテスト用エクスポート
保存済み一覧の上部 **「JSONをコピー」** → 1〜3着入力済みのレースがまとめてクリップボードにコピーされる（コピーできない端末では画面に表示されるので全選択してコピー）。
これを Claude に渡すか、リポジトリの `data/samples/YYYY-MM-DD_レース名.json` として保存すると `npm run backtest` の対象になります。

---

## 7. トラブルシューティング

| 症状 | 原因と対処 |
|:--|:--|
| ホーム画面のアイコンから開けない／404 | GitHub リポジトリが非公開になっている。Settings → General → Danger Zone → Change visibility → Public。続けて Settings → Pages で main / (root) を確認 |
| 真っ黒な画面 | index.html の JS エラー。PC で `npm run check` を通したファイルだけを main に置く。直前の良いコミットに戻す |
| 見た目が崩れる（色・余白が消える） | Tailwind CDN に届いていない。機能は動く。通信状態を確認 |
| 取込で「形式が正しくありません」 | JSON に `race` と `horses` が無い、または貼り付け時に前後に余計な文字がある。```json の記号ごと貼らない |
| 保存済みレースが消えた | ブラウザのサイトデータ削除・別ブラウザ・シークレットモード。大事なレースは「JSONをコピー」で外に出しておく |
| 買い目が出ない | 有効な馬（馬番・推定人気・単勝人気が入っている）が 4 頭未満 |
| ⚠️ が出て買えない | 仕様。見送りが正解として記録する |

---

## 8. PC 側のコマンド（開発・検証・バックテスト）

前提: Git と Node.js（LTS）。初回のみ `git clone https://github.com/iamshojikenchiku-rgb/smart-yosou.git && cd smart-yosou && npm install`。

| コマンド | 内容 |
|:--|:--|
| `npm run check` | 構文チェック → 外部参照チェック → jsdom 描画。**index.html を変更したら必ず** |
| `npm test` | check ＋ G層テスト ＋ エクスポートテスト ＋ judge 一致テスト |
| `node scripts/judge.js 入力.json --md --ledger data/ledger.json` | ツールと同じ判定を CLI で出す（週次メモ用 Markdown） |
| `npm run backtest` | `data/samples/` の集計（10レース未満は結論を出さない） |
| `node scripts/ledger.js summary` | 収支台帳の月次集計 |
| `node scripts/ledger.js add --date … --race … --purchased … --returned …` | 台帳に追記 |

---

## 9. バージョンアップの進め方（Claude Code / Cowork に任せる場合）

任せられます。ただし「何を変えるか」を決めるのは人で、Claude は設計書どおりに実装・検証します。手順は固定です。

1. **設計書（アドendum）を書く**: `docs/` に「目的・変更範囲・変更禁止事項・検証方法」を書く。書けない変更は実装しない。既存のアドendum（G層π v0.1）が雛形。
2. **依頼する**: Claude Code（PC）または Cowork（クラウド）で「`docs/<アドendum>` の範囲のみを実装。CLAUDE.md の禁止事項を厳守。`feature/<名前>` ブランチで1モジュール1コミット。`npm run check` が通ってから次へ。main へのマージは私が確認してから」と指示する。
3. **Claude が行うこと**: 設計書と CLAUDE.md を読む → 実装 → `npm test` → コミット → 設計書との食い違いは実装せず `docs/実装メモ` に記録して質問。
4. **人が行うこと**: PR の差分と実装メモを確認 → main にマージ → 公開URLとタブレットで表示確認。
5. **戻し方**: 問題があれば `git revert` または直前の良いコミットへ。GitHub Pages は main の内容がそのまま公開されるので、マージまで本番には影響しない。

Claude に**任せてはいけないこと**: 荒れスコア・軸信頼度・🛑🚫⚠️の閾値・予算ティア・買い目ロジックの変更を「気を利かせて」行うこと。CLAUDE.md で禁止しており、Claude は提案メモに留めます。変えたい場合は人がアドendumに書いてから。

補足: このセッションのように Cowork から直接 push できない場合は、バンドル／zip で受け取り GitHub にアップロードして PR を作る運用になります（今回の手順どおり）。Cowork 側でリポジトリを「ソース」に追加すれば直接 push できます。
