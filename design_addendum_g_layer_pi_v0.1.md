# 設計アドendum — G層「π参考値」／荒れレース暦拡張／バックテスト基盤（v0.1）

作成日: 2026-09-05
対象: Smart予想ツール `index.html`（自己完結版・A〜F層搭載）
用途: Claude Cowork への実装依頼書。本書の範囲外の変更は行わない。

---

## 0. 鉄則（変更禁止事項）

- 既存の荒れスコア・軸信頼度・🛑/🚫/⚠️閾値・予算ティア・買い目ロジックは**一切変更しない**。
- 本アドendumで追加するものは「表示」と「検証基盤」のみ。**購入判定には影響させない**。
- 1/π（≈31.83%）に競馬上の必然性はない。**「一定値を基準にしたフィルター」として検証する対象**であり、有効性はバックテストで確認するまで未確定。
- 既定の対象は **3着内率（ワイド基準）**。単勝勝率ではない。
- JSXソース相当の編集 → Babel構文チェック → jsdom実描画 → 納品（既存ルール）。

---

## 1. モジュール1：`ARERU_RACES` 拡張（本番反映可）

既存テーブルに以下を追記。既存エントリと重複する場合は既存を優先し、備考のみ補完。

| 月 | レース | 区分 | 備考 |
|:--|:--|:--|:--|
| 2 | フェブラリーS | dirt | 伏兵頻出 |
| 3 | 高松宮記念 | sprint | 毎年荒れる |
| 5 | ヴィクトリアマイル | mile_f | 牝馬古馬・穴が出やすい |
| 9 | スプリンターズS | sprint | 展開次第 |
| 10 | 秋華賞 | mid_f | 荒れ年あり |
| 10 | 京都大賞典 | long | G2・荒れやすい |
| 11 | エリザベス女王杯 | mid_f | 評価難 |
| 12 | チャンピオンズC | dirt | 穴出現率高 |
| 12 | 阪神C | sprint | 年末で荒れ多め |

- 4月（クラシック路線）は「大穴狙い控えめ」として**減点フラグ**（`caution: true`）を持たせる。荒れスコアには加算しない。
- 各行に `source: "perplexity_2026-09"` を付け、後日の根拠追跡を可能にする。

---

## 2. モジュール2：G層「π参考値」（表示のみ）

### 2.1 目的
血統スコア（順序尺度）を**暫定的に3着内率へ変換**し、公平オッズと市場オッズを並べて表示する。
将来の較正（モジュール3）に向けた「観察用の窓」。判定には使わない。

### 2.2 スキーマ追加（任意キー・後方互換）
- `odds_fuku`（複勝オッズ下限。数値。任意）。無ければ率のみ表示。
- 既存16キーは変更なし。17キー目として扱う。

### 2.3 暫定変換式（較正前・v0.1）
レース内の全頭 i について、血統スコア合計 `S_i`（A〜F層）を用いる。

```
z_i   = (S_i − mean(S)) / sd(S)          // sd=0 のときは z_i=0
w_i   = exp(k · z_i)                      // k = 0.5（初期値・要較正）
p_i   = 3 · w_i / Σ w                     // 3着内率の期待和＝3 で正規化
p_i   = min(p_i, 0.95)
fair_i = 1 / p_i                          // 公平複勝オッズ
```

- `k` は定数として1箇所に置き、モジュール3で較正後に差し替える。
- 表示ラベルは必ず「**暫定・較正前**」を付す。

### 2.4 π判定（表示のみ）
- 条件A: `p_i ≥ 1/π (0.3183)`
- 条件B: `odds_fuku_i ≥ π (3.1416)`（`odds_fuku`が無い場合は判定保留 "—"）
- A∧B → 「π候補」、Aのみ → 「率のみ」、それ以外 → 空欄。
- **🛑/🚫/⚠️ が出たレースは π候補が出ても買わない**（鉄則）。UI上も🛑時はπ列をグレーアウト。

### 2.5 表示位置
馬別テーブルに列を3つ追加：`推定3着内率` / `公平複勝` / `π判定`。
既存の列・並び順・色分けは変更しない。

---

## 3. モジュール3：バックテスト基盤

### 3.1 データ形式
ツールに「結果入力」を追加（レース単位、1〜3着の馬番と確定馬場を手入力）し、
既存の入力JSON＋各層スコア＋結果を1レース1オブジェクトで**エクスポート**できるようにする。

```json
{
  "race": "しらさぎS", "date": "2026-06-21", "courseType": "big_turf",
  "baba": "稍重", "result": [10, 7, 11],
  "horses": [ { "uno":10, "score_total":..., "score_layers":{...}, "odds":..., "odds_fuku":..., "est_pop":... }, ... ]
}
```

- 既存のアドendumに記録済みのレース（オークス、しらさぎS、日本ダービー、安田記念、宝塚記念 等）は、
  Coworkが設計文書から**手動で遡及入力**してサンプル化する。

### 3.2 集計スクリプト（Node.js・単体ファイル）
入力: エクスポートJSONの配列。出力: 以下の指標。

1. π条件（A∧B）該当馬の**複勝回収率・的中率・該当件数**
2. 条件Aのみ／Bのみの同指標（分解確認）
3. `k` を 0.2〜1.0 で走査し、対数損失が最小となる `k`（較正値）
4. スコア帯別（10点刻み）の実3着内率（較正カーブ）

### 3.3 採用基準（先に固定）
- サンプル **10レース未満は結論を出さない**（戦略見直し検討メモ v0.1 と同一規律）。
- 10レース以上で、π条件該当馬の複勝回収率が**100%超かつ該当件数≥15**の場合のみ、
  「π候補を買い目候補に含めるか」を**別アドendumで**検討する。
- 基準未達なら G層は表示のまま維持し、蓄積を続ける。

---

## 4. 実装順序と検証

| # | 作業 | 検証 |
|:--|:--|:--|
| 1 | モジュール1（ARERU_RACES追記） | 追記レース名で荒れスコアが変化することを確認 |
| 2 | モジュール2（G層表示） | 種牡馬名なし・`odds_fuku`なしのJSONで既存表示が崩れないこと／🛑時グレーアウト |
| 3 | モジュール3（結果入力・エクスポート・集計） | 過去5レースを遡及入力し、集計が走ること |
| 4 | 全体 | Babel構文チェック → jsdom描画 → GitHub Pages反映 |

---

## 5. 記録

- 本書は「買い場を増やす提案」ではなく「検証窓の設置」である。
- バックテスト結果が出るまで、週次運用フロー（7ステップ）は変更しない。
